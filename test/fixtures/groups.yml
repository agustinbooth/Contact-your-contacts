# == Schema Information
#
# Table name: groups
#
#  id         :bigint           not null, primary key
#  group_name :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  user_id    :integer
#

one:
  group_name: MyString

two:
  group_name: MyString
